name: agendamento_servicos
description: "Agendamento de serviços para oficinas mecânicas."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.4
  flutter_datetime_picker: ^1.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.0
  test: ^1.25.7
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true